CREATE TABLE foodNutrients(
    db_id INTEGER GENERATED BY DEFAULT AS IDENTITY,
    id UUID PRIMARY KEY NOT NULL,
    amount INTEGER NOT NULL,
    foodId UUID REFERENCES food(id) ON DELETE CASCADE NOT NULL,
    nutrientId UUID REFERENCES nutrients(id) ON DELETE CASCADE NOT NULL
);